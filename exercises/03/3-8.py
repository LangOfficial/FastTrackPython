# 3-8. Research Methods  .find() and .index()

# Both locate a substring within a string
# .find() returns -1 if the substring isn't in the string
# .index() throws a ValueError if the substring isn't found